# Preserve the plugin defaults and ignore Starship prompt infrastructure too.
ignored_processes = ["ls", "cat", "pwd", "clear", "git", "direnv", "scutil", "starship"]

[process_aliases]
# Keep Beads and Hub Viewer commands grouped under the AI board label.
bd = "ai board"
bv = "ai board"
wbd = "ai board"
wbv = "ai board"
